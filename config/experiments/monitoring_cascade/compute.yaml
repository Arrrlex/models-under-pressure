# @package _global_
defaults:
  - /probe: mean  # or any other probe type

# Model configuration
model_names:
  #- llama-70b
  #- gemma-27b
  #- gemma-12b
  - llama-8b
  - llama-1b
  - gemma-1b

# Probe configuration
probe_model_name: null
probe_layer: null

# Dataset configuration
eval_datasets: null #["toolace", "mt", "mts", "mask"]
train_dataset_path: training/original_doubled_unconfounded/train.jsonl
max_samples: null
batch_size: 4

# Computation settings
compute_activations: true  # If false, activations will be read from the store
output_dir: data/results/monitoring_cascade
